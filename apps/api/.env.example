DATABASE_URL="postgresql://klinikpi:CHANGE_ME@localhost:5433/klinikpi?schema=public"
JWT_SECRET="CHANGE_ME_min_32_random_chars_openssl_rand_base64_48"
JWT_EXPIRES_IN="7d"
PORT=4000
TZ=Asia/Tashkent
NODE_ENV=development

# Comma-separated origins for production
CORS_ORIGIN="http://localhost:3000"

# OpenAI (AI hisobotlar)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Telegram bot + Mini App initData validation
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Seed (development only)
# SEED_WIPE=true  — wipes ALL data before seed (never in production)
# SEED_ON_START=true — ignored if users exist
