# Browser uses same-origin /api proxy (see next.config.js rewrites)
API_PROXY_TARGET=http://127.0.0.1:4000

# Demo login UI — productionda false
NEXT_PUBLIC_DEMO=false

# Optional: direct API URL (usually leave empty for proxy)
NEXT_PUBLIC_API_URL=
